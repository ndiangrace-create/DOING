# DOING GitHub Cleanup｜2026-08-24

目的：停止 GitHub 因歷史分支、Actions artifacts、版本快照與退休頁持續膨脹。

清理前盤點：
- Repository Git size：約 23,523 KB。
- Branches：173 條。
- 舊未完成 PR：#7、#137、#156（已關閉，不合併）。
- Market Chromium 驗證每次約產生 1.2 MB 圖片 artifact，原本可保留約 90 天。
- Site／Production／DNS workflow 另有 14～90 天 artifact。
- root 存有多份 WORLD_TREE／CHANGELOG／CHECKPOINT／AUDIT 版本快照。
- `legacy-pages/` 存有已退休頁，其中舊 admin.html 約 584 KB；只有 Booking source 仍被正式使用。

本次定案：
1. 正式長期 branch 只保留 main。
2. 合併時一次刪除現有 Actions artifacts 與舊完成 workflow runs。
3. 所有正常 workflow 停止上傳驗收圖片、網站 build、部署 JSON、DNS evidence artifact。
4. Booking source 搬至 `modules/booking/`；`legacy-pages/` 完全退出。
5. 歷史 UAT HTML 快照退出；保留即時 E2E scripts。
6. 歷史版本文件集中 `docs/archive/2026-08/`。
7. 世界樹／ChangeLog 後續只更新目前 SSOT，不再每次另存 Vxx 日期版。
8. `doing-test-records.json` 保留為最近一次基準，不因 archive／CI-only 整理強迫產生新 checkpoint；正式 CI 仍每次即時執行。
9. 2BL 不在此次範圍，未修改。
