# DOING v20 真人 UAT／Click-through 驗收清單

更新：2026-08-20 15:07（Asia/Taipei）

## 固定驗收規則

每一條流程都必須依序確認：**入口 → 按鈕 → 頁面 → 權限 → API → Supabase → 完成狀態 → 下一步／返回**。

- 程式級 PASS 不等於真人瀏覽器 PASS。
- LINE OAuth、手機相機、第三方付款、LINE 內建瀏覽器與正式通知，必須保留真人 UAT 狀態，不可用模擬結果冒充。
- 同一流程最多只能有一層正式主框架。
- `smart-application.html` 是唯一正式營運申請頁。
- `member.html` 與 `about.html` 只允許做相容轉址，不得再承載完整舊 UI。
- 所有正式設定與營運資料以 Supabase 為 SSOT；前端不得永久保存正式設定。
- 同一功能只能有一個正式名稱與一個正式資料根；**「我的報名」**是會員報名功能唯一正式名稱，主資料固定為 `registrations`，正式會員查詢 API 固定為 `getMyRegsGlobal`。
- 「我的紀錄」不得再出現在客服、前台按鈕、信件模板或正式操作指引；相容路由不得建立第二份資料。
- 未驗證／未完成且沒有正式關聯的暫存資料保留 90 天後批次清理；正式會員、工作空間、報名、付款、退款、財務及持續使用資料不受此規則刪除。

## 角色 1｜一般使用者／報名者

1. 首頁 → 找活動。
2. 點活動卡 → 正確活動內容，不得跳別場或申請營運帳號。
3. 報名 → 正確帶入場次、品牌／會員資料。
4. 送出 → 建立同一筆正式 `registrations`。
5. 我的報名 → `member-panel.html#activities`。
6. 查看審核、付款、位置、通知、報到、退款等狀態。
7. 客服詢問「我要去哪裡看報名？」→ 回答必須使用「我的報名」，不得說「我的紀錄」。
8. 返回 → 仍回本人報名流程，不進工作空間。

真人 UAT：LINE 登入、正式付款外部跳轉、手機報到。

## 角色 2｜品牌使用者

1. LINE 登入 → 我的 DOING。
2. 我的品牌 → 建立／編輯品牌。
3. 同名品牌只提示，不自動合併。
4. 邀請／申請加入品牌 → 品牌管理者確認。
5. 成員重新整理後權限仍正確。

真人 UAT：本人 LINE 接受邀請、手機品牌編輯。

## 角色 3｜營運申請者

1. 申請營運帳號 → `smart-application.html`。
2. 工作多選 → 必要角色補問。
3. 圖像式系統預覽 → 使用者確認理解。
4. 才出現正式姓名／品牌／電話／Email／FB／IG。
5. Facebook 粉專或 Instagram 至少一個。
6. LINE 驗證必須離開 iframe，在 top-level OAuth。
7. LINE 驗證完成 → 一般情況**自動建立工作空間**，不再等待平台逐筆人工核准。
8. 自動建立固定沿用 `tenant_apply_logs → tenants / tenant_settings / staff`；核准模組寫入 `tenant_settings.module_flags_json`，不得另開同功能資料表。
9. 只有身分衝突、必要資料異常或建立失敗 → `platform_risk_cases` → 平台人工複核。
10. 開通後 → 我的 DOING → 正確工作空間。

真人 UAT：LINE OAuth、自動開通後進入我的 DOING、刻意製造可控風險案例確認人工複核（不得以正式使用者資料做破壞性測試）。

## 角色 4｜租戶擁有者／主辦管理員

1. 我的 DOING／工作空間 → 正確租戶。
2. 只顯示已開通模組。
3. 工作日曆 → 月／週／列表。
4. 營運項目／場次 → 建立、編輯、公開。
5. 報名 → 審核 → 付款 → 設備／資源 → 排位 → 通知。
6. 財務 → 收入／退款／支出 → 結案。
7. 正式設定重新整理後仍由 Supabase 回讀，不得依賴瀏覽器暫存。
8. 服務型預約 → 月曆與預約表顯示同一筆 `registrations / timeslots / booking_calendars` 正式資料。
9. 模組停用 → 舊資料保留；重新開啟 → 原紀錄可繼續使用，不建立第二套資料。
10. 系統設定 → 下載全部營運資料；只有租戶擁有者可操作，帳號鎖定仍可下載，且不得包含其他租戶或登入憑證。
11. 返回 → 回原租戶，不跳平台首頁或別租戶。

真人 UAT：真實場次新增、第一筆服務預約與重新整理讀回、完整資料下載、正式收退款、真實結案、手機後台。

## 角色 5｜現場工作人員／實際出攤者

1. 現場登入 → 只能看到授權場次。
2. 搜尋報名者 → 正確名單。
3. 報到／押金／現場狀態 → 寫入正式紀錄。
4. 出攤成員 → 本人 LINE 身分。
5. 申請撤場／完成 → 狀態回到我的報名與主辦後台。

真人 UAT：真實手機、LINE 內建瀏覽器、現場通行碼。

## 角色 6｜平台總管

1. 平台登入 → 保持 platform 身分。
2. 一般營運申請不再逐筆人工核准；只處理 `platform_risk_cases` 風險／例外複核。
3. 租戶 → 正確指定租戶、模組與健康狀態。
4. 問題中心 → 深連結指定場次／訂單。
5. 處理後 → 稽核紀錄可追溯。
6. 返回平台 → 不要求重登、不遺失總管狀態。

真人 UAT：正式總管登入、風險例外複核、手機平台頁。

## 90 天暫存資料治理 UAT

1. 未完成 LINE／Google 驗證、且 `tenant_id is null` 的暫存申請滿 90 天 → 可被批次清理。
2. 已建立 workspace／tenant 的申請 → 不可被 90 天清理。
3. 純匿名曝光／點擊事件，且沒有 `registration_id`，滿 90 天 → 可被批次清理。
4. 已完成報名歸因、有 `registration_id` 的事件 → 保留。
5. `registrations / payments / refunds / finance / tenants / staff / 已登入會員正式資料` → 清理函式不得刪除。
6. 每日批次最多 1000 筆，避免長交易拖慢正式操作；函式上限 2000。
7. 重複的 `idx_tenant_apply_logs_status_created_v2` 移除，只留單一有效索引；清理查詢使用局部索引。

## 9 條營運世界樹主幹

1. 一般使用者報名流程。
2. 品牌使用者流程。
3. 營運申請到工作空間（LINE 驗證後自動開通；例外才人工複核）。
4. 工作空間與工作日曆。
5. 主辦後台完整營運。
6. 現場操作。
7. 平台總管處理閉環（例外／風險為主）。
8. 資料來源與租戶隔離。
9. 新舊架構與單層畫面防回歸。

## DoD

- 自動回歸全部 PASS。
- 任何已知 P0／P1 路由或資料錯誤為 0。
- 「我的報名」名稱、`getMyRegsGlobal` API、`registrations` 主資料根一致。
- 所有正式設定可由 Worker 讀取並寫回 Supabase。
- 預約月曆與預約表必須共用正式資料根，不可建立第二套預約資料夾。
- 租戶擁有者可隨時下載自己的完整營運資料；停用模組、帳號鎖定或停止付費不得阻止資料取回。
- SECURITY DEFINER 內部 trigger 函式不得開放給 `anon` 或 `authenticated` 直接執行。
- 一般營運申請在 LINE 驗證後可自動建立唯一工作空間；異常才進人工複核。
- 90 天清理只碰暫存／匿名無正式關聯資料，正式使用資料保留。
- 舊頁面只保留 redirect-only 相容入口，不存在第二套正式 UI。
- 真人 UAT 尚未執行的項目必須標示為「待真人驗證」，不得標成 PASS。
